1 initialiser variable msg = a l'argument 1 (comment le recup?)
2 for (int i = 0; i < len(msg); i++)
    if (msg[i] est upper)
        msg[i].lower()
    else
        msg[i].upper()
3 print le msg avec for (int i = len(msg); i > 0; i--)

